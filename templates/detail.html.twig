{% extends "main.html.twig" %}

{% block header %}

    <script>
        $(document).ready( function() {

            const mySlides = [
                {src: "assets/img/gerechten/{{ data.0.gerecht.afbeelding}}"}
            ] // Einde slide sources
                    
            $("#background-image").vegas({                               // let op de #!!! dit is een ID voor later en hier moet dus een #
                overlay: 'assets/vegas/overlays/01.png',                        // Mogelijk deze weghalen als we die pixel overlay niet willen hebben
                slides: mySlides,
            }) // Einde background-image functie

        }) // Einde .ready functie
    </script>

{% endblock %}


{% block content %}
<div class="col-md-5" style="background-image: url('assets/img/gerechten/{{data.0.gerecht.afbeelding}}')">   {#kolom met plaatje #}
</div>


<div class="col-md-7" style="background-color: #FFF">
    {# cijfers van personen, prijs en calorieen #}
    <div class="row">
        <div class="col-md-2"> 
            <i class="fa fa-file-pdf" style="color:#F00"></i> 4
        </div>
        <div class="col-md-2"> 
            <i class="fa fa-file-pdf" style="color:#F00"></i> {{ data.0.prijs |round(2)}}
        </div>
        <div class="col-md-2"> 
            <i class="fa fa-file-pdf" style="color:#F00"></i> {{ data.0.calorieen |round(2)}}
        </div>
    </div>

    {# Naam en sterren #}


    {#% set record = data %#}
    <div class="row">
        <div class="col-md-8"> 
            <h2> {{ data.0.gerecht.titel }} </h2>
        </div>
        <div class="col-md-4"> 
            <h3> **** </h3>
        </div>
    </div>

    <div class="row">
        <div class="col-md-3">
            <h4> Keuken </h4> 
        </div>
        <div class="col-md-3">
            keuken 
        </div>

        <div class="col-md-3">
            <h4> Type </h4> 
        </div>
        <div class="col-md-3">
            Type 
        </div>
    </div>

    {# omschrijving #}
    <div class="row">
    Omschrijving
    </div>

    {# op lijst en favorieten knop #}
    <div class="row">
        <div class="col-md-8">
        Knop
        </div>
        <div class="col-md-4">
        favoriet
        </div>
    </div>

</div> {# Einde rechter kant #}

       <pre>
        {{ dump(data.0.gerecht) }} 
        {#{{dump data.0}} #}
       </pre>
{% endblock %}
